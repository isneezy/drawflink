{
  "name": "url-shortener-api",
  "version": "0.0.1",
  "description": "Url shortening service backed by express.js",
  "scripts": {
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "tsc": "tsc",
    "build": "run-p lint tsc",
    "dev": "nodemon --watch src -e ts --exec run-s build start",
    "start": "node dist/index.js"
  },
  "author": "Ivan Vilanculo",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "consola": "^2.14.0",
    "express": "^4.17.1",
    "mongose": "^0.0.2-security",
    "nodemon": "^2.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/node": "^14.0.22",
    "npm-run-all": "^4.1.5",
    "tslint": "^6.1.2",
    "typescript": "^3.9.6"
  }
}
